/** Default hero background when admin has not uploaded a custom image. */
export const BRANDING_CONFIG = {
  defaultHeroGradient:
    'linear-gradient(165deg, rgba(255,255,255,0.92) 0%, rgba(248,249,251,0.88) 42%, #f8f9fb 100%)',
  backgroundPath: 'assets/branding/homepage-background',
  supportedExtensions: ['jpg', 'jpeg', 'png', 'webp'],
  maxBytes: 10 * 1024 * 1024,
};
