// Optional API base when the kiosk is static (e.g. GitHub Pages) but email/sync
// runs on a separate Node server. Leave empty when using `npm start` (same origin).
export const JOBS_CONFIG = {
  apiBaseUrl: '',
};
